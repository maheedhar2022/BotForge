# Server Port
PORT=5000

# Security (JWT secret)
JWT_SECRET=super-secret-key-change-in-production

# OpenAI API (Optional, RAG falls back to robust local semantic search if empty)
OPENAI_API_KEY=

# Environment (development / production)
NODE_ENV=development
